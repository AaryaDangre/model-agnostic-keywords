# Scripts

Utilities for working with the keyword files.

compile_keywords
- Usage: `python -m scripts.compile_keywords`
- Produces `prompt-keywords/combined_keywords.json` and `prompt-keywords/combined_keywords.yml` (YAML output requires `PyYAML`).

Install requirements (optional):

```bash
python -m pip install pyyaml
```
